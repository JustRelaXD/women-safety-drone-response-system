# Base URL of the planner API. Default "/api" is proxied by the Vite dev
# server to VITE_API_TARGET (default http://localhost:8000).
VITE_API_BASE_URL=/api
# Dev-server proxy target for /api/* (only used by the Vite dev server).
VITE_API_TARGET=http://localhost:8000
